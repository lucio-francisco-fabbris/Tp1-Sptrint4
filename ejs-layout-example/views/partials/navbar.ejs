<nav class="navbar">
        <ul class="ul">
            <% navbar.forEach(link => { %>
                    <li>
                        <a href="<%= link.href %>">
                            <%= link.text %>
                        </a>
                    </li> 
                <% }); %>
        </ul>
</nav>